{
  "name": "bsc-management-system",
  "version": "1.0.0",
  "description": "Enterprise Balanced Scorecard Management System for Insurance Company",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && npx prisma db seed && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "seed": "cd backend && npx prisma db seed",
    "test": "cd backend && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
